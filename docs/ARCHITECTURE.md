# هيكل التطبيق - Architecture Documentation

## 📐 نظرة عامة على البنية

تطبيق بنك الأفكار مبني باستخدام نمط **MVC (Model-View-Controller)** مع Flask:

- **Model**: النماذج في `app.py` (User, Idea, Comment, Visit, OAuth)
- **View**: القوالب في مجلد `templates/`
- **Controller**: المسارات (Routes) في `app.py`

## 🔄 تدفق البيانات

### 1. تسجيل الدخول التقليدي

```
المستخدم → /login (POST) → التحقق من البيانات → تسجيل الدخول → إعادة التوجيه إلى الصفحة الرئيسية
```

### 2. تسجيل الدخول عبر Google

```
المستخدم → /login/google → Google OAuth → /login/google/authorized →
إنشاء/تحديث المستخدم → تسجيل الدخول → إعادة التوجيه إلى الصفحة الرئيسية
```

### 3. إضافة فكرة جديدة

```
المستخدم المسجل → /submit_idea (POST) → إنشاء فكرة جديدة → حفظ في قاعدة البيانات →
إعادة التوجيه إلى الصفحة الرئيسية
```

### 4. عرض فكرة

```
المستخدم → /idea/<id> → جلب الفكرة من قاعدة البيانات → زيادة عدد المشاهدات →
عرض الفكرة والتعليقات
```

### 5. إضافة تعليق

```
المستخدم المسجل → /idea/<id>/comment (POST) → إنشاء تعليق جديد →
حفظ في قاعدة البيانات → إعادة التوجيه إلى صفحة الفكرة
```

### 6. تسجيل الزيارات

```
كل طلب → @app.before_request → log_visit() →
تحديد IP, المتصفح, الجهاز → حفظ في قاعدة البيانات
```

## 🗂 هيكل الملفات التفصيلي

### app.py - الملف الرئيسي

#### الأقسام الرئيسية:

1. **الاستيرادات (Imports)**

```python
# Flask الأساسي
from flask import Flask, render_template, request, redirect, url_for, flash, send_from_directory

# قاعدة البيانات
from flask_sqlalchemy import SQLAlchemy

# إدارة الجلسات
from flask_login import LoginManager, UserMixin, login_user, login_required, logout_user, current_user

# OAuth
from flask_dance.contrib.google import make_google_blueprint, google
from flask_dance.consumer import oauth_authorized
from flask_dance.consumer.storage.sqla import SQLAlchemyStorage

# معالجة الملفات
from werkzeug.utils import secure_filename
import uuid
```

2. **الإعدادات (Configuration)**

```python
# إعدادات Flask
app.config['SECRET_KEY'] = os.urandom(24)  # مفتاح سري للجلسات
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///bank_of_ideas.db'  # قاعدة البيانات
app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False  # تعطيل التتبع التلقائي

# إعدادات Google OAuth
app.config['GOOGLE_OAUTH_CLIENT_ID'] = '...'
app.config['GOOGLE_OAUTH_CLIENT_SECRET'] = '...'
app.config['SERVER_NAME'] = 'localhost:4000'

# إعدادات رفع الملفات
app.config['UPLOAD_FOLDER'] = 'static/uploads'
app.config['MAX_CONTENT_LENGTH'] = 16 * 1024 * 1024  # 16MB
```

3. **النماذج (Models)**

**نموذج User:**

- يمثل المستخدمين في النظام
- يدعم تسجيل الدخول التقليدي و Google OAuth
- الحقول:
  - `id`: معرف فريد
  - `username`: اسم المستخدم (فريد)
  - `email`: البريد الإلكتروني (فريد)
  - `password`: كلمة المرور (مشفرة، nullable للـ OAuth)
  - `google_id`: معرف Google (nullable)
  - `bio`: السيرة الذاتية
  - `profile_picture`: اسم ملف الصورة الشخصية
  - `full_name`: الاسم الكامل
  - `location`: الموقع
  - `website`: الموقع الإلكتروني
  - `is_admin`: صلاحيات الأدمن (Boolean, default=False)
  - `created_at`: تاريخ الإنشاء
- العلاقات:
  - `ideas`: علاقة one-to-many مع Idea
  - `comments`: علاقة one-to-many مع Comment
  - `visits`: علاقة one-to-many مع Visit

**نموذج Idea:**

- يمثل الأفكار المنشورة
- الحقول:
  - `id`: معرف فريد
  - `title`: عنوان الفكرة
  - `description`: وصف الفكرة
  - `category`: التصنيف
  - `created_at`: تاريخ الإنشاء
  - `views`: عدد المشاهدات (default=0)
  - `user_id`: معرف المستخدم (صاحب الفكرة)
- العلاقات:
  - `author`: علاقة many-to-one مع User
  - `comments`: علاقة one-to-many مع Comment

**نموذج Comment:**

- يمثل التعليقات على الأفكار
- الحقول:
  - `id`: معرف فريد
  - `content`: محتوى التعليق
  - `created_at`: تاريخ الإنشاء
  - `updated_at`: تاريخ التعديل (nullable)
  - `is_published`: حالة النشر (Boolean, default=True)
  - `user_id`: معرف المستخدم (صاحب التعليق)
  - `idea_id`: معرف الفكرة
- العلاقات:
  - `author`: علاقة many-to-one مع User
  - `idea`: علاقة many-to-one مع Idea

**نموذج Visit:**

- يمثل سجل الزيارات والإحصائيات
- الحقول:
  - `id`: معرف فريد
  - `ip_address`: عنوان IP
  - `user_agent`: User-Agent string
  - `browser`: نوع المتصفح (Chrome, Firefox, etc.)
  - `device_type`: نوع الجهاز (Mobile, Tablet, Desktop)
  - `page_path`: مسار الصفحة
  - `referrer`: المرجع (nullable)
  - `user_id`: معرف المستخدم (nullable للزوار)
  - `created_at`: تاريخ ووقت الزيارة
- العلاقات:
  - `user`: علاقة many-to-one مع User (nullable)

**نموذج OAuth:**

- يخزن معلومات OAuth للمستخدمين
- يستخدمه Flask-Dance لإدارة جلسات Google
- الحقول:
  - `id`: معرف فريد
  - `provider`: مزود OAuth (Google)
  - `provider_user_id`: معرف المستخدم في المزود
  - `token`: الرمز المميز (JSON)
  - `user_id`: معرف المستخدم

4. **Google OAuth Integration**

```python
# إنشاء Blueprint لـ Google OAuth
google_bp = make_google_blueprint(
    client_id=...,
    client_secret=...,
    scope=[...],  # الصلاحيات المطلوبة
    storage=SQLAlchemyStorage(OAuth, db.session, user=current_user)  # تخزين الرموز في قاعدة البيانات
)

# معالج تسجيل الدخول
@oauth_authorized.connect_via(google_bp)
def google_logged_in(blueprint, token):
    # 1. الحصول على معلومات المستخدم من Google
    # 2. البحث عن المستخدم في قاعدة البيانات
    # 3. إنشاء مستخدم جديد إذا لم يكن موجوداً
    # 4. تسجيل الدخول
```

5. **Middleware لتسجيل الزيارات**

```python
@app.before_request
def log_visit():
    # تجنب تسجيل ملفات static
    # تجنب تسجيل زيارات dashboard و admin
    # تجنب تسجيل زيارات الأدمن
    # تحديد IP, المتصفح, الجهاز
    # حفظ في قاعدة البيانات
```

6. **المسارات (Routes)**

**الصفحة الرئيسية (`/`):**

- يعرض صفحة ترحيبية مع معلومات عن المنصة
- روابط سريعة للصفحات المختلفة

**صفحات الأفكار:**

- `/most-viewed`: الأكثر مشاهدة
- `/latest-ideas`: الأحدث إضافة
- `/most-commented`: الأكثر تعليقاً

**تسجيل الدخول (`/login`):**

- GET: عرض نموذج تسجيل الدخول
- POST: التحقق من البيانات وتسجيل الدخول

**التسجيل (`/register`):**

- GET: عرض نموذج التسجيل
- POST: التحقق من البيانات وإنشاء حساب جديد

**إضافة فكرة (`/submit_idea`):**

- يتطلب تسجيل الدخول (`@login_required`)
- GET: عرض نموذج إضافة فكرة
- POST: حفظ الفكرة في قاعدة البيانات

**عرض فكرة (`/idea/<id>`):**

- جلب الفكرة من قاعدة البيانات
- زيادة عدد المشاهدات
- عرض الفكرة والتعليقات (منشورة فقط للزوار، جميعها لصاحب الفكرة)

**إدارة التعليقات:**

- `/idea/<id>/comment` (POST): إضافة تعليق
- `/comment/<id>/edit` (GET, POST): تعديل تعليق
- `/comment/<id>/delete` (POST): حذف تعليق
- `/comment/<id>/toggle-publish` (POST): نشر/إخفاء تعليق (لصاحب الفكرة)

**البروفايل:**

- `/profile`: عرض البروفايل
- `/profile/edit`: تعديل البروفايل (GET, POST)
- `/uploads/<filename>`: عرض الصور المرفوعة

**لوحة التحكم (للأدمن):**

- `/dashboard`: لوحة التحكم الرئيسية
  - إحصائيات عامة
  - إحصائيات الزيارات
  - إحصائيات المتصفحات والأجهزة
  - أكثر الصفحات زيارة
  - آخر الزيارات (مع Pagination)

**إدارة المستخدمين (للأدمن):**

- `/admin/users`: عرض قائمة المستخدمين
- `/admin/users/add`: إضافة مستخدم جديد (GET, POST)
- `/admin/users/<id>/edit`: تعديل مستخدم (GET, POST)
- `/admin/users/<id>/toggle-admin`: تغيير صلاحيات المستخدم (POST)
- `/admin/users/<id>/delete`: حذف مستخدم (POST)

## 🔐 الأمان

### تشفير كلمات المرور

```python
# عند التسجيل
hashed_password = generate_password_hash(password)

# عند تسجيل الدخول
check_password_hash(user.password, password)
```

### حماية المسارات

```python
@login_required  # يتطلب تسجيل الدخول
def submit_idea():
    ...

# التحقق من صلاحيات الأدمن
if not current_user.is_admin:
    flash('ليس لديك صلاحية', 'danger')
    return redirect(url_for('home'))
```

### OAuth Security

- استخدام HTTPS في الإنتاج
- تخزين الرموز بشكل آمن في قاعدة البيانات
- التحقق من صحة الرموز

### معالجة رفع الملفات

```python
# التحقق من نوع الملف
ALLOWED_EXTENSIONS = {'png', 'jpg', 'jpeg', 'gif', 'webp'}

# استخدام secure_filename
filename = secure_filename(file.filename)
unique_filename = f"{uuid.uuid4()}_{filename}"
```

## 📊 قاعدة البيانات

### العلاقات (Relationships)

```
User (1) ──── (N) Idea
User (1) ──── (N) Comment
User (1) ──── (N) Visit
Idea (1) ──── (N) Comment
User (1) ──── (N) OAuth
```

### الاستعلامات (Queries)

**الأكثر مشاهدة:**

```python
Idea.query.order_by(Idea.views.desc()).all()
```

**الأحدث إضافة:**

```python
Idea.query.order_by(Idea.created_at.desc()).all()
```

**الأكثر تعليقاً:**

```python
db.session.query(
    Idea,
    db.func.count(Comment.id).label('comment_count')
).join(Comment).group_by(Idea.id).order_by(
    db.desc('comment_count')
).all()
```

**Pagination للزيارات:**

```python
page = request.args.get('page', 1, type=int)
visits_pagination = Visit.query.order_by(
    Visit.created_at.desc()
).paginate(page=page, per_page=20, error_out=False)
```

**إحصائيات المتصفحات:**

```python
db.session.query(
    Visit.browser,
    db.func.count(Visit.id).label('count')
).group_by(Visit.browser).all()
```

## 🎨 الواجهة الأمامية

### القوالب (Templates)

**base.html:**

- القالب الأساسي الذي تمتد منه جميع القوالب
- يحتوي على Navbar و Footer
- يحتوي على روابط CSS و JS
- دعم RTL كامل

**home.html:**

- صفحة ترحيبية
- روابط سريعة
- معلومات عن المنصة

**view_idea.html:**

- يعرض تفاصيل الفكرة
- نموذج إضافة تعليق (للمستخدمين المسجلين)
- قائمة التعليقات مع أزرار التعديل/الحذف (لصاحب التعليق)
- أزرار نشر/إخفاء (لصاحب الفكرة)

**dashboard.html:**

- لوحة تحكم للأدمن
- بطاقات إحصائية
- جداول تفصيلية
- Pagination للزيارات

**admin_users.html:**

- جدول بجميع المستخدمين
- إحصائيات سريعة
- أزرار الإجراءات (تعديل، تغيير صلاحيات، حذف)

### CSS المخصص

```css
:root {
  --primary-color: #4a90e2;
  --secondary-color: #ffb84d;
  --accent-color: #4ecdc4;
  --success-color: #6bcb77;
  --warning-color: #ffd93d;
  --background-color: #f8f9fa;
  --text-color: #2c3e50;
}

/* استخدام متغيرات CSS للألوان */
/* تأثيرات hover سلسة */
/* تصميم متجاوب */
/* دعم RTL */
```

### JavaScript

**main.js:**

- تحديث السنة تلقائياً في Footer
- معاينة الصورة الشخصية قبل الرفع

## 🔄 دورة حياة الطلب (Request Lifecycle)

1. **الطلب (Request)**

   - المستخدم يزور صفحة
   - Flask يستقبل الطلب

2. **Middleware**

   - `@app.before_request` → `log_visit()`
   - تسجيل الزيارة (إن لم تكن static/admin/dashboard)

3. **المعالجة (Processing)**

   - Flask يحدد المسار المناسب
   - تنفيذ الكود في الدالة المرتبطة بالمسار
   - التحقق من الصلاحيات (`@login_required`, `is_admin`)
   - جلب البيانات من قاعدة البيانات (إذا لزم الأمر)

4. **الاستجابة (Response)**
   - رندر القالب مع البيانات
   - إرسال HTML للمتصفح

## 🛠 التقنيات المستخدمة

### Backend

- **Flask 3.0.2**: إطار عمل الويب
- **SQLAlchemy 3.1.1**: ORM لقاعدة البيانات
- **Flask-Login 0.6.3**: إدارة جلسات المستخدمين
- **Flask-WTF 1.2.1**: معالجة النماذج
- **Flask-Dance 7.0.0**: تكامل OAuth مع Google
- **Werkzeug 3.0.1**: أداة WSGI وتشفير كلمات المرور
- **oauthlib 3.2.2**: مكتبة OAuth 2.0
- **requests-oauthlib 1.3.1**: دعم OAuth لـ requests

### Frontend

- **Bootstrap 5.3.0**: إطار عمل CSS
- **Bootstrap Icons 1.7.2**: مكتبة الأيقونات
- **Jinja2**: محرك القوالب
- **IBM Plex Sans Arabic**: خط عربي احترافي
- **JavaScript (Vanilla)**: للتفاعلات الديناميكية

### Database

- **SQLite**: قاعدة بيانات خفيفة وسهلة الاستخدام

## 📈 الإحصائيات والتتبع

### تتبع الزيارات

- يتم تتبع كل زيارة تلقائياً عبر `@app.before_request`
- يتم تحديد:
  - IP Address
  - نوع المتصفح (Chrome, Firefox, Safari, Edge, Opera)
  - نوع الجهاز (Mobile, Tablet, Desktop)
  - مسار الصفحة
  - المرجع (Referrer)
  - المستخدم (إن كان مسجل دخول)

### الإحصائيات المتاحة

- إجمالي الزيارات
- زيارات اليوم/الأسبوع/الشهر
- توزيع المتصفحات
- توزيع الأجهزة
- أكثر الصفحات زيارة
- IPs فريدة

## 🚀 التحسينات المستقبلية

1. **البحث والفلترة**

   - إضافة بحث في الأفكار
   - فلترة حسب التصنيف
   - فلترة حسب التاريخ

2. **التقييمات**

   - إضافة نظام تقييم للأفكار
   - عرض متوسط التقييمات

3. **المشاركة**

   - إضافة أزرار مشاركة على وسائل التواصل الاجتماعي

4. **الإشعارات**

   - إشعارات عند إضافة تعليقات جديدة
   - إشعارات عند نشر/إخفاء التعليقات

5. **التحسينات الأمنية**

   - Rate limiting
   - CSRF protection محسّن
   - Input validation أقوى

6. **الميزات الإضافية**
   - تعديل وحذف الأفكار
   - نظام الإشارات (Tags)
   - المفضلة (Favorites)
   - المتابعة (Follow)

---

**آخر تحديث**: ديسمبر 2025
