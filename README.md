# بنك الأفكار - Bank of Ideas

تطبيق ويب تفاعلي يسمح للمستخدمين بمشاركة أفكارهم والتفاعل مع أفكار الآخرين.

## 🚀 البدء السريع

### المتطلبات

- Python 3.8 أو أحدث
- pip

### التثبيت والتشغيل

```bash
# إنشاء بيئة افتراضية
python3 -m venv venv

# تفعيل البيئة الافتراضية
source venv/bin/activate  # على macOS/Linux
# أو
venv\Scripts\activate  # على Windows

# تثبيت المتطلبات
pip install -r requirements.txt

# تشغيل التطبيق
python3 app.py
```

افتح المتصفح وانتقل إلى: `http://localhost:4000`

### تشغيل باستخدام Docker

#### الطريقة الأولى: Docker Compose (موصى بها)

```bash
# بناء وتشغيل الحاوية
docker-compose up --build

# للتشغيل في الخلفية
docker-compose up -d --build

# إيقاف الحاوية
docker-compose down
```

#### الطريقة الثانية: Docker مباشرة

```bash
# بناء الصورة
docker build -t bank-of-ideas .

# تشغيل الحاوية
docker run -d -p 4000:4000 \
  -v $(pwd)/instance:/app/instance \
  -v $(pwd)/static/uploads:/app/static/uploads \
  --name bank-of-ideas \
  bank-of-ideas

# عرض السجلات
docker logs -f bank-of-ideas

# إيقاف الحاوية
docker stop bank-of-ideas

# حذف الحاوية
docker rm bank-of-ideas
```

بعد التشغيل، افتح المتصفح وانتقل إلى: `http://localhost:4000`

## 📚 التوثيق

- **[دليل الاستخدام](docs/USAGE.md)** - كيفية استخدام التطبيق بالتفصيل
- **[هيكل التطبيق](docs/ARCHITECTURE.md)** - شرح البنية والكود التفصيلي
- **[دليل قاعدة البيانات](docs/DATABASE.md)** - كيفية إعداد وإدارة قاعدة البيانات

## ✨ المميزات

### للمستخدمين

- ✅ تسجيل الدخول (تقليدي أو Google OAuth)
- ✅ إنشاء حساب مع بروفايل شخصي
- ✅ رفع صورة شخصية
- ✅ نشر أفكار جديدة مع تصنيفات
- ✅ نظام تعليقات تفاعلي
- ✅ تعديل وحذف التعليقات الخاصة
- ✅ عرض الأفكار الأكثر مشاهدة والأحدث والأكثر تعليقاً
- ✅ إحصائيات (المشاهدات والتعليقات)
- ✅ صفحات منفصلة للأفكار (الأكثر مشاهدة، الأحدث، الأكثر تعليقاً)

### للأدمن

- ✅ لوحة تحكم شاملة مع إحصائيات مفصلة
- ✅ إدارة المستخدمين (عرض، تعديل، حذف، إضافة)
- ✅ نظام صلاحيات (مستخدم عادي / أدمن)
- ✅ تتبع الزيارات (IP، المتصفح، نوع الجهاز)
- ✅ إحصائيات الزيارات (اليوم، الأسبوع، الشهر)
- ✅ إحصائيات المتصفحات والأجهزة
- ✅ أكثر الصفحات زيارة
- ✅ Pagination للزيارات (20 سجل في كل صفحة)

### المميزات التقنية

- ✅ تصميم متجاوب (Responsive Design)
- ✅ دعم RTL (Right-to-Left) للغة العربية
- ✅ واجهة مستخدم حديثة وجذابة
- ✅ تسجيل الزيارات تلقائياً
- ✅ نظام نشر/إخفاء التعليقات (لصاحب الفكرة)

## 🛠 التقنيات المستخدمة

### Backend

- **Flask 3.0.2** - إطار عمل الويب
- **SQLAlchemy 3.1.1** - ORM لقاعدة البيانات
- **Flask-Login 0.6.3** - إدارة جلسات المستخدمين
- **Flask-WTF 1.2.1** - معالجة النماذج
- **Flask-Dance 7.0.0** - تكامل OAuth مع Google
- **Werkzeug 3.0.1** - أداة WSGI وتشفير كلمات المرور
- **oauthlib 3.2.2** - مكتبة OAuth 2.0
- **requests-oauthlib 1.3.1** - دعم OAuth لـ requests

### Frontend

- **Bootstrap 5.3.0** - إطار عمل CSS للتصميم المتجاوب
- **Bootstrap Icons 1.7.2** - مكتبة الأيقونات
- **Jinja2** - محرك القوالب
- **IBM Plex Sans Arabic** - خط عربي احترافي
- **JavaScript (Vanilla)** - للتفاعلات الديناميكية

### Database

- **SQLite** - قاعدة بيانات خفيفة وسهلة الاستخدام

### نماذج البيانات (Models)

- **User** - المستخدمون (مع دعم Google OAuth)
- **Idea** - الأفكار المنشورة
- **Comment** - التعليقات على الأفكار
- **Visit** - سجل الزيارات والإحصائيات
- **OAuth** - معلومات OAuth للمستخدمين

## 📁 هيكل المشروع

```
bank/
├── app.py                    # الملف الرئيسي للتطبيق
├── requirements.txt          # المتطلبات
├── README.md                 # هذا الملف
├── .gitignore               # ملفات Git المستثناة
├── docs/                    # التوثيق
│   ├── ARCHITECTURE.md      # هيكل التطبيق التفصيلي
│   └── USAGE.md             # دليل الاستخدام
├── templates/               # قوالب HTML
│   ├── base.html            # القالب الأساسي
│   ├── home.html            # الصفحة الرئيسية
│   ├── login.html           # تسجيل الدخول
│   ├── register.html        # التسجيل
│   ├── submit_idea.html     # إضافة فكرة
│   ├── view_idea.html       # عرض فكرة
│   ├── profile.html         # البروفايل
│   ├── edit_profile.html    # تعديل البروفايل
│   ├── dashboard.html       # لوحة التحكم (للأدمن)
│   ├── admin_users.html     # إدارة المستخدمين
│   ├── admin_add_user.html  # إضافة مستخدم
│   ├── admin_edit_user.html # تعديل مستخدم
│   ├── edit_comment.html    # تعديل تعليق
│   ├── most_viewed.html     # الأكثر مشاهدة
│   ├── latest_ideas.html    # الأحدث إضافة
│   └── most_commented.html  # الأكثر تعليقاً
├── static/                  # الملفات الثابتة
│   ├── css/
│   │   └── style.css        # ملف CSS المخصص
│   ├── js/
│   │   └── main.js          # ملف JavaScript
│   ├── uploads/             # الصور المرفوعة
│   └── favicon.svg          # أيقونة الموقع
├── instance/                # قاعدة البيانات
│   └── bank_of_ideas.db     # قاعدة بيانات SQLite
└── venv/                    # البيئة الافتراضية (لا يُرفع)
```

## 🔐 الأمان

- تشفير كلمات المرور باستخدام Werkzeug
- حماية المسارات بـ `@login_required`
- التحقق من الصلاحيات للأدمن
- معالجة آمنة لرفع الملفات
- OAuth 2.0 مع Google

## 📊 الإحصائيات والتتبع

التطبيق يتتبع تلقائياً:

- عدد الزيارات لكل صفحة
- IP Address للزوار
- نوع المتصفح (Chrome, Firefox, Safari, etc.)
- نوع الجهاز (Mobile, Tablet, Desktop)
- الصفحات الأكثر زيارة
- إحصائيات المستخدمين (الأفكار، التعليقات، الزيارات)

## 🎨 التصميم

- تصميم متجاوب يعمل على جميع الأجهزة
- دعم كامل للغة العربية (RTL)
- ألوان حديثة وجذابة
- خط عربي احترافي (IBM Plex Sans Arabic)
- أيقونات Bootstrap Icons

## 💾 قاعدة البيانات

### التطوير (Development)
- **SQLite**: الخيار الافتراضي - لا يحتاج إعداد

### الإنتاج (Production)
- **PostgreSQL**: موصى به للإنتاج
- راجع [دليل قاعدة البيانات](docs/DATABASE.md) للتفاصيل الكاملة

### مع Docker
```bash
# docker-compose.yml يتضمن PostgreSQL تلقائياً
docker-compose up --build
```

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT.

---

**تم التطوير بـ ❤️ باستخدام Flask و Python**

**آخر تحديث**: ديسمبر 2025
