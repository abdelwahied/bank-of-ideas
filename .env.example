# Secret Key - يجب تغييره في الإنتاج
# يمكن توليد مفتاح آمن باستخدام: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-secret-key-in-production

# Database URL - للإنتاج استخدم PostgreSQL
# SQLite للتطوير: sqlite:///bank_of_ideas.db
# PostgreSQL للإنتاج: postgresql://user:password@localhost:5432/bank_of_ideas
DATABASE_URL=sqlite:///bank_of_ideas.db

# Google OAuth Credentials
# احصل على هذه البيانات من Google Cloud Console
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret

# Flask Environment
FLASK_ENV=development

# OAuth Insecure Transport (للتنمية فقط - لا تستخدم في الإنتاج!)
# OAUTHLIB_INSECURE_TRANSPORT=1

# PostgreSQL Credentials (للإنتاج)
# POSTGRES_DB=bank_of_ideas
# POSTGRES_USER=bank_user
# POSTGRES_PASSWORD=your-secure-password
